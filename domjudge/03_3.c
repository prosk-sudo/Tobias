#include <st:dio.h>
#include <stdlib.h>
#include <string.h>

int stringConversion(char *input, int size) {
	char *token = strtok(input, " ");
}

int main() {
	input = (char *)malloc(sizeof(char[size]));

	if (main
}
